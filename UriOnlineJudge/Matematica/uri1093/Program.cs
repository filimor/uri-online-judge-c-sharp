// Incompleto

using System;

namespace uri1093 // Vampiros
{
    internal static class Program
    {
        private static void Main()
        {
            //string[] str;
            //string[] zeros = { "0", "0", "0", "0" };

            //while ((str = Console.ReadLine().Split(' ')) != zeros)
            //{
            //    int.TryParse(str[0], out int ev1);
            //    int.TryParse(str[1], out int ev2);
            //    int.TryParse(str[2], out int at);
            //    int.TryParse(str[3], out int d);

            //    double probAcerto = at / 6.0;
            //}
        }
    }
}